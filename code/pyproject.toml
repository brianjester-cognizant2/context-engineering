[project]
name = "ce"
version = "1.0.0"
description = "Runnable reference harness for the Context Engineering for AI course"
requires-python = ">=3.10"
dependencies = []          # standard library only, by design

[tool.setuptools.packages.find]
include = ["ce*"]
